import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const NoDataDisplay = styled.p`
${tw`text-center text-red-600	`}`;

const EnterButton = styled.p`
${tw`text-green-600 text-center text-xl	`}`

export {  NoDataDisplay, EnterButton }